#include "student.h"
#include <math.h>


#include <algorithm>

using namespace std;


bool Compare(const Student& first, const Student& second) {
  return first.rating - second.rating > 0;
}


int main() {
	return 0;
}
